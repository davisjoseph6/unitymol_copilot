# UnityMol Copilot Configuration

# LLM configuration
llm:
  provider: ollama
  model: deepseek-coder-v2:16b
  parameters:
    temperature: 0.7
    top_p: 0.9
    max_tokens: 2048

# Logging configuration
logging:
  level: INFO
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
